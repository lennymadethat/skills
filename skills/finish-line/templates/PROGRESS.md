# Progress — <project> (the loop's memory; newest at the top)

Every run starts by reading this file and checks.json. Every slice ends by appending five lines here: what was built, what the checker said, what is parked and why, tokens used, next.

## Log
- <date time>Z — loop started · turn 0 · <n> checks FAIL
